# Phase 0 environment — Python 3.11 venv on Apple Silicon (MPS).
# Pinned to versions verified working with torch.backends.mps.is_available() == True.
torch==2.12.1
torchvision==0.27.1
numpy==2.3.5
scikit-learn==1.9.0
matplotlib==3.11.0          # Grad-CAM colormap (app/gradcam.py)

# Phase 6 (interactive app).
# gradio 4.x requires pillow<11, so Pillow is pinned to 10.4.x (was 12.0.0).
pillow==10.4.0
gradio>=4.0,<5.0
# gradio 4.44.x imports HfFolder, removed in huggingface_hub 1.0 — pin below 1.0.
huggingface_hub>=0.24,<1.0
# gradio 4.44.1 doesn't cap fastapi/starlette; the resolver otherwise pulls
# fastapi 0.138/starlette 1.x, whose TemplateResponse signature change breaks
# gradio's page render (Jinja2 "unhashable type: dict"). Pin a contemporary set.
fastapi==0.112.4
starlette==0.38.6
